{{#each data}}

    <div class="row offer">
        <div class="row">
            <div class="col-md-6">
                <h3>{{title}}</h3>
                <p>posted by {{user}} si na data 2107</p>
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <img src={{img}}>
            </div>
            <div class="col-md-8">
                <p class="text">{{text}}</p>
            </div>
        </div>
        <button class="btn btn-default pull-right">More</button>
    </div>


{{/each}}